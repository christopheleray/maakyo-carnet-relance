<p id="notice"><%= notice %></p>
<div class = "container main-container">
  <h1>Liste de vos établissements</h1>

  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @locations.each do |location| %>
        <tr>
          <td><%= location.name %></td>
          <td><%= link_to 'Voir', location %></td>
          <td><%= link_to 'Modifier', edit_location_path(location) %></td>
          <td><%= link_to 'Supprimer', location, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Location', new_location_path %>
</div>